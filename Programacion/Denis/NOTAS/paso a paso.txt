npm init -inicialisarlo
 instalar dependencias

express instalar npm i expresss --save
npm mprgan --save -dependencia que peticiones se hace al servidor web que protocolodiirdir
npm i --D nodemon --save ejecutar codigo y se reinicie si funciona o no
npm i ejs --save motor de palantillas

middleware dentro dos procesos
como se utiliza ejs

npm run dev

bootstrap
responsiva
formulario
